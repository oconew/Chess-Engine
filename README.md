# Chess-Engine

A simple chess engine that builds a gametree from a given state of a chess game and looks n moves into the future. Plsyer can play against the engine and it iwll attempt to beat them at chess. Difficulty can be scaled by changing the number n of moves forward into the future the engine looks; this also increases compute time exponentially.
